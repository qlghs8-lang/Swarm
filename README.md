# Swarm

**8방향 이동 로그라이크 서바이버** · Unity 6000.4.3f1 (URP) · C#

> 시간 기반 웨이브로 몰려오는 적 무리(swarm)를 헤쳐나가며 레벨업 카드로 성장하고, 10분 뒤 등장하는 보스를 처치해 라운드를 클리어하는 서바이버 액션. 원래 1개월 스코프로 시작했지만, 포트폴리오 볼륨을 위해 캐릭터 시스템·직업별 무기·무기 진화·상점·보스전 등을 원 스코프 밖으로 계속 확장 중.

---

## 게임플레이 흐름

```
Title 화면(캐릭터 선택/해금 + 골드 상점) → 게임 시작
                    ↓
플레이어 이동(8방향) → 적 처치 → 경험치·골드 오브젝트 드랍(랜덤 산포) → 습득
                    ↓
    경험치: 레벨업 카드 3장 중 1장 선택 (무기 강화 카드 + 런 패시브 카드가 한 풀에서 섞여 나옴)
                    ↓
      상시 스폰(기본 적 + 4분부터 탱커) — 시간 흐를수록 스폰 속도·적 체력/방어 증가
                    ↓
   90초부터 90초 간격으로 대규모 웨이브 6회("빠른 무리" / "WAVE n" 안내) → 10분에 보스 등장(BOSS 안내)
                    ↓
      보스 처치 → 클리어 결과 화면 / 사망·15분 시간 초과 → 게임 오버 결과 화면 → "다시 시작" 또는 "타이틀로"
                    ↓
항아리(시작 30개 + 시간에 따라 증가, 시야 밖 생성): 한 방에 파괴 → 경험치/힐팩/골드/자석 드랍
                    ↓
골드: 적 처치 또는 항아리에서 낮은 확률로 드랍 → 세션 간 유지(PlayerPrefs)
      → 타이틀 화면 상점에서 영구 패시브 구매/캐릭터 해금 (다음 판부터 적용)
```

---

## 캐릭터 & 무기

각 캐릭터는 **자기 전용 무기 3개**(시작 무기 1개 + 레벨업 카드로 얻는 무기 2개)를 갖는다. 무기는 캐릭터 간에 공유되지 않으며, 데미지는 그 캐릭터의 주 능력치(공격력 또는 마력)를 따른다. 세 캐릭터는 전투 스타일뿐 아니라 기본 스탯(체력/방어력/이동속도/쿨타임 감소)도 서로 다르게 설계되어 정체성이 나뉜다.

| 캐릭터 | 해금 | 주 능력치 | 정체성 | 시작 무기 | 카드 무기 1 | 카드 무기 2 |
| ------ | ---- | --------- | ------ | --------- | ----------- | ----------- |
| 전사   | 기본 해금 | 공격력 | 근거리 딜탱 — 체력·방어력 높음, 스킬셋이 단순한 대신 맷집으로 버팀 | 전방 베기 (근접 부채꼴, 실제 판정과 정확히 일치하는 시각 인디케이터) | 공전검 (플레이어 주위를 도는 검, 투사체 증가 패시브를 먹으면 검이 늘어나고 항상 균등 배치) | 흡혈 (패시브 — 어떤 무기든 적을 때릴 때마다 10% 확률로 그 적에게 추가 강타 피해 + 체력 회복. 회복만 별도 쿨타임을 걸어 적이 많을수록 무한 회복되는 것을 막음) |
| 궁수   | 골드 100G | 공격력 | 유틸형 — 이동속도·쿨타임 감소 보너스, 체력·방어력은 중간 | 활쏘기 (사거리 내 서로 다른 적 3명을 자동 조준해 동시에 한 발씩, 투사체 증가 패시브로 발수 증가) | 화살비 (논타겟 부채꼴 다발 투사체) | 대시 (액티브 — 온스크린 버튼/스페이스바로 바라보는 방향으로 짧게 치고 나가는 무적 회피 이동) |
| 마법사 | 골드 150G | 마력   | 범위 딜러 — 잡몹 처리에 강하지만 단일 대상엔 약함, 체력·방어력 낮음 | 낙뢰 (범위 내 여러 적 머리 위로 번개, 레벨업할수록 동시 타격 인원 증가) | 화염구 (관통형 대형 투사체) | 힐 (일정 주기마다 체력 회복) |

- 모든 무기는 `LevelableWeapon` 추상 클래스를 상속한다 — 레벨(최대 8)이 오를수록 데미지가 늘고 쿨타임이 줄며, 레벨업 카드를 뽑으면 그 무기의 레벨이 오른다. 데미지 없는 유틸리티 무기(대시, 힐)도 같은 프레임워크를 그대로 써서 "레벨↑ = 쿨타임↓"만 적용받는다.
- 무기가 레벨 8(만렙)에 도달하면 다음 레벨업 때 그 무기를 완전히 대체하는 **진화 카드**가 강제로 등장한다(자세한 내용은 아래 "무기 진화" 참고).
- `CharacterDefinition`이 캐릭터 적용 시 그 캐릭터가 실제로 들고 있는 무기들만 "이번 판에 등장 가능"으로 표시하기 때문에, 다른 캐릭터의 무기가 레벨업 카드에 섞여 나오지 않는다. 같은 시점에 캐릭터별 기본 스탯 보너스(체력/방어력/이동속도/쿨타임 감소)도 함께 적용된다.
- 활쏘기·화염구처럼 "범위 내 여러 적에게 동시 타격"이 필요한 무기와, 화살비처럼 "논타겟 부채꼴"이 필요한 무기는 서로 다른 클래스로 완전히 분리되어 있다 — 같은 발사체 개념이라도 겨냥 방식(멀티 타겟 vs 고정 스프레드)이 다르면 로직을 억지로 하나로 합치지 않는다.
- 투사체 개수·범위 크기를 늘리는 런 패시브는 모든 무기가 공용으로 소비하는 범용 스탯(`ProjectileCountBonus`/`AreaSizeBonus`)이라, 무기별로 별도 구현 없이 값만 읽어 쓴다.

---

## 무기 진화

9개 무기 전부 레벨 8(만렙) 도달 시 진화 카드로 완전히 다른 무기로 전환된다. 진화 후엔 레벨 1부터 새로 성장하며(기존 레벨과 무관), 원본 무기는 비활성화(`Retire()`)된다.

| 캐릭터 | 원본 | 진화 | 핵심 변화 |
| ------ | ---- | ---- | -------- |
| 전사 | 전방 베기 | 콤보 어택 | 베기→찌르기→내려찍기 3연속 자동 컴보(순서대로 데미지 증가, 레벨업 시 데미지 증가+주기 단축) |
| 전사 | 공전검 | 소드 체이싱 | 평소엔 공전, 사거리 내 적 감지 시 스치듯 날아가 타격 후 다른 적으로 이동 반복(투사체 수만큼 서로 다른 적을 노려 한곳에 뭉치지 않음), 화면 밖으로 나가면 즉시 텔레포트 귀환 |
| 전사 | 흡혈 | 레이지 | 프록 조건은 흡혈과 같되 회복에 쿨타임이 없고, 프록할 때마다 10% 확률로 공격력·최대 체력 영구 스택 획득 — HP 바 옆 버프 아이콘 줄에 스택 수 표시 |
| 궁수 | 활쏘기 | 집중 | 여러 적에 한 발씩 분산 → 가장 가까운 적 하나에게 전탄 집중 연사(기본 3발, 투사체 증가 패시브로 발수 증가) |
| 궁수 | 화살비 | 난사 | 관통(데미지 감소 없음, 대신 기본 데미지 낮춤) + 360도 회전 발사로 전환 |
| 궁수 | 대시 | 맹독 대시 | 대시한 자리에 독가스 장판을 남겨 도트딜 + 방어력/이동속도 감소(발동 방식은 원본과 동일하게 액티브 유지) |
| 마법사 | 낙뢰 | 아이스 볼트 | 범위 즉발 다중 타격 → 실제 투사체로 전환, 맞은 적 기준 주변 적으로 연쇄 전이(기본 2회 + 2레벨마다 1회), 명중한 적은 이동속도 30% 감소(1.5초) |
| 마법사 | 화염구 | 익스플로전 | 대상 위치로 운석이 대각선으로 낙하해 착탄 피해 + 화염 장판 생성(밟으면 발화, 장판을 벗어나도 몇 초간 도트딜 지속) |
| 마법사 | 힐 | 블레싱 | 회복 + 짧은 시간(3초) 마력/방어력/마법관통력 증가 및 다른 무기 쿨타임 감소 버프 부여(블레싱 자신의 쿨타임은 이 버프에서 제외해 무한 루프 방지) |

- 진화 여부와 무관하게 같은 UI/입력이 계속 작동하도록 인터페이스로 분리했다 — 예: 대시/맹독 대시 둘 다 `IActiveRoll`을 구현해서, 회피 버튼 UI는 "지금 활성화된 쪽"을 몰라도 되고 진화 전후로 끊김이 없다.
- 독가스 장판·화염 장판처럼 지속형 범위 디버프/도트 효과는, 여러 개가 겹쳐도 도트딜·디버프가 중복 적용되지 않고 "하나만 맞은 것"과 동일하게 처리된다 — 효과의 소유권을 장판이 아니라 적 쪽의 단일 상태(`EnemyHealth.ApplyPoison`/`ApplyBurn`)로 옮겨서, 몇 개가 동시에 겹쳐 신호를 보내도 데미지 틱은 하나만 나간다.

---

## 스탯

| 스탯 | 획득 경로 | 설명 |
| ---- | --------- | ---- |
| 공격력 / 마력 | 런 패시브 + 영구 패시브 | 캐릭터의 주 능력치에 맞는 쪽이 자동으로 적용 |
| 최대 체력 | 런 패시브 + 영구 패시브 | |
| 이동속도 | 런 패시브 + 영구 패시브 | |
| 방어력 | 런 패시브 + 영구 패시브 | 받는 피해 감소 (최대 90%) |
| 쿨타임 감소 | 런 패시브 + 영구 패시브 | 모든 무기 쿨타임에 곱연산 적용 |
| 자석 | 영구 패시브 | 경험치/골드 픽업 자동 흡인 반경 |
| 행운 | 런 패시브 + 영구 패시브 | **크리티컬 확률**. 카드 5레벨×5% + 상점 10레벨×2.5%로 합계 50%가 상한. 크리티컬은 공격 1회당 한 번만 굴려서 범위 공격이 터지면 맞은 모두가 함께 터진다 — 숫자가 뒤섞여 흩어지지 않고 한 방 크게 들어간 것으로 읽힌다. 크리티컬 배율은 200%, 데미지 숫자는 금색+확대로 표시. 도트 딜과 고정 데미지 프록(흡혈/레이지)은 의도적으로 제외 |
| 투사체 증가 | 런 패시브 | 발사체/공전형 무기의 개수 +1 (레벨당) — 여러 무기가 공용으로 소비하는 범용 스탯 |
| 범위 증가 | 런 패시브 | 범위형 무기의 반경 +15% (레벨당) — 여러 무기가 공용으로 소비하는 범용 스탯 |
| 관통력 | 런 패시브 + 영구 패시브 | 카드/상점 모두 하나만 존재하지만, 올리면 물리관통력·마법관통력이 동시에 오름(내부적으로는 두 스탯으로 분리 관리 — 나중에 적별 물리/마법 방어를 따로 조절하거나 특정 무기에만 관통력을 붙이는 등 벨런싱 확장을 위한 사전 설계) |

**런 패시브**와 **영구 패시브**는 이름은 비슷하지만 완전히 별개 시스템이다.
- **런 패시브**: 레벨업 카드로 뽑아서 무기 카드와 같은 풀에서 섞여 나오고, 이번 판이 끝나면 초기화된다.
- **영구 패시브**: 골드로 타이틀 화면 상점에서 구매하고, `PlayerPrefs`에 영구 저장되어 다음 판에도 유지된다.

레벨업 카드 3장을 뽑을 때 **이미 레벨이 오른(투자한) 무기/패시브에는 가중치를 더 줘서** 먹었던 걸 더 잘 뽑히게 한다 — 이것저것 다 찍먹하기보다 한 무기를 밀어서 키우는 선택도 유효한 전략이 되도록 하기 위함.

---

## 웨이브 & 보스

보스가 등장하는 10분까지가 한 판의 본편이고(못 잡으면 15분에 강제 종료), 시간 흐름에 따라 난이도가 계단식으로 올라간다.

- **상시 스폰**: 기본 적이 처음부터 계속 나오고, 240초부터 탱커가 섞인다(가중치 5:2). **빠름 적은 상시 스폰 풀에 들어 있지 않고 아래 "빠른 무리" 웨이브로만 등장한다** — 상시로 깔면 빠른 적이 늘 붙어 있어 위협이 평준화되고, 몰아서 던져야 "한 방향에서 빠른 무리가 온다"가 사건으로 읽힌다. 스폰 간격은 0.3초에서 0.1초까지(300초에 걸쳐) 빨라지고, 적 체력은 최대 2.5배, 물리 방어력과 마법 방어력 모두 최대 25%까지(600초에 걸쳐) 함께 증가한다 — 두 방어력을 분리해 둔 덕분에 마법사 진화(관통력)의 실효성이 시간이 지날수록 의미를 갖는다.
- **스폰 위치**: 60% 확률로 플레이어가 **가는 방향 쪽 180도 호**에 스폰된다. 사방에 고르게 뿌리면 한 방향으로 직진하는 것만으로 절반이 떨어져 나가 애초에 포위될 수가 없다. 동시 생존 수는 400마리가 상한이고, 22유닛 밖으로 처진 적은 지워지지 않고 플레이어 주변으로 재배치된다.
- **대규모 웨이브**: 90초부터 90초 간격으로 총 6회, 두 종류가 번갈아 온다. 상시 스폰과 별개로 추가되는 물량이고, 스폰 상한도 무시하고 들어온다.
  - **"빠른 무리"**(90/270/450초, 30→45→60마리): 빠름 적만, 무작위 한 방향 ±45도에 몰아서 — 한쪽 벽이 밀려오는 형태다.
  - **"WAVE 1~3"**(180/360/540초, 40→60→80마리): 상시 스폰 풀에서 추첨해 사방에서.
- **보스**: 10분 시점에 "BOSS" 안내와 함께 보스가 등장하고, 화면 상단에 전용 체력바가 뜬다. 이 시점부터는 **10분 생존이 아니라 보스 처치가 클리어 조건**이며, 보스 등장 후에도 기존 잡몹 스폰은 계속되어 보스와 잡몹을 동시에 상대해야 한다. 보스는 전용 스프라이트/Walk 애니메이션을 갖고, 추적+접촉 데미지에 더해 **패턴 2종**을 쓴다.
  - **슬램**: 보스 근처를 원형 경고로 예고한 뒤 광역 타격 — 붙어서 때리는 플레이를 처벌한다.
  - **운석**: 플레이어가 서 있는 자리로 돌 3개가 순차 낙하(경고 후 착탄, 약간의 조준 오차). 아레나 전역에 닿으므로 원거리 빌드가 구석에서 무시할 수 없고, 해법은 언제나 "계속 움직이기"로 동일하다.
  - 경고 원은 `BossTelegraph`가 공통으로 그리며(Decal 레이어, 실제 판정 반경과 일치), 패턴은 각자 독립 타이머로 돌되 `BossPatternGate`(`IBossPattern.IsCasting`)를 거쳐 **두 패턴이 같은 프레임에 겹쳐 터지지 않는다** — 플레이어가 자기 잘못 없이 두 원을 동시에 피해야 하는 상황을 막는다.
  - 보스는 `EnemyStatusImmunity`로 **경직성 군중제어(넉백·스턴)에 면역**이다. 매 타격마다 밀리거나 기절하면 슬램을 끝내지 못해 전투가 "가만히 서서 사격 버튼 홀드"로 무너지기 때문. 반면 둔화·도트·방어력 감소는 그대로 들어가서 해당 빌드는 계속 유효하다.
  - 보스 체력바는 채우기 게이지가 아니라 **체력 구간별 스프라이트(BossBar_State0~3) 교체** 방식이다.
  - 밸런싱용으로 에디터·개발 빌드에서는 **F2로 보스를 즉시 소환**할 수 있다(직렬화된 스폰 시각을 건드리지 않는다).
- **시간 제한**: 보스 등장 후 타이머가 남은 시간 카운트다운으로 바뀌고, 60초 전에 안내 배너로 경고한다. 15분에 도달하면 강제 게임 오버 — 보스를 잡지 않고 버티며 무한히 파밍하는 플레이를 막는 안전장치다. 스폰 속도는 5분에 상한을 치고 그대로 유지되므로, 그 이후로 길어질수록 밸런싱된 구간에서 멀어지기만 한다.
- 웨이브/보스 스폰은 모두 `EnemySpawner`가 소유하며, 이벤트(`OnWaveTriggered`/`OnBossSpawned`)로 `GameManager`(클리어 판정)와 UI(안내 배너, 보스 체력바)에 알린다 — 스포너는 "언제 무엇이 나오는지"만 책임지고, 그에 대한 반응(승리 조건, 화면 연출)은 전부 구독자 쪽에서 처리한다.

---

## 아레나

맵은 **반지름 70유닛의 원형 아레나**로 제한된다. 무한 맵에서는 한 방향으로 계속 도망치는 것이 언제나 최적해가 되는데, 그러면 근접·광역 무기가 원거리 무기보다 구조적으로 불리해지고 "포위당해 죽는" 실패 패턴 자체가 성립하지 않는다. 경계가 있어야 방향 전환이 강제되고, 그 순간에 군중을 밀어내고 뚫고 나가는 플레이가 의미를 갖는다.

- 크기 기준은 화면이 아니라 **적이 실제로 존재하는 범위**다. 리사이클 거리가 22유닛이라 활성 범위 지름이 약 44유닛이고, 아레나는 그 3배 남짓이다. 기본 이동속도 4 기준 중앙에서 벽까지 17.5초, 가로지르는 데 35초 — 도망이 유효 전술로 남되 영구 해법은 못 된다.
- 경계는 벽 콜라이더가 아니라 **위치 클램프**다. 콜라이더로 막으면 군중의 밀어내기 힘이 플레이어를 경계 밖으로 밀어내고, 벽에 몰릴 때마다 "뚫고 나가기" 메커니즘과 충돌한다. 클램프하면서 바깥 방향 속도 성분만 제거해 벽을 따라 미끄러지게 한다.
- 플레이어가 벽에 붙으면 스폰 링의 절반이 아레나 밖이 되는데, 이때 오프셋을 안쪽으로 반사시킨다. 적이 돌담 뒤에 얌전히 생기는 게 아니라 열린 쪽에서 몰려오므로 **구석에 몰리는 것이 실제로 위험해진다**.
- **항아리(파괴 가능 오브젝트)**가 아레나에 흩어져 있다. 어느 무기로든 한 방에 깨지고, 깨지면 반드시 무언가를 떨어뜨린다 — 돌아다닌 것에 대한 보상이므로 "꽝"을 두면 안전한 자리를 떠난 게 손해였다고 가르치는 셈이 된다.
  - **개수가 고정이 아니라 늘어난다**: 시작 30개(첫 1분에 항아리밭을 걷는 느낌이 나지 않도록 일부러 얇게) + 20초마다 1개 + 60초마다 5개. 개수를 일정하게 유지하면 10분째 아레나의 가치가 1분째와 똑같아지고, 이미 깬 항아리가 새 항아리를 막는 셈이 된다. 늘어나게 두면 자리를 비운 사이 뒤쪽 땅이 다시 채워져서 **한 바퀴 도는 값어치가 시간이 갈수록 커진다** — 난이도 곡선이 움직이는 방향과 같다.
  - 새 항아리는 항상 **플레이어 시야 밖(18유닛 이상)** 에 생긴다. 아레나가 넓어서 무작위로 놔도 화면 안에 걸릴 확률은 50번에 한 번 정도지만, 그 한 번이 "게임이 머리 위로 전리품을 떨궜다"로 읽힌다 — 걸어가서 만난 것과 찾아온 것의 차이다.
  - 깨진 항아리는 치우지도 대체하지도 않는다. 마지막 프레임이 잔해로 그대로 남아서 **"여긴 이미 지나갔다"가 한눈에 읽히고**, 항아리가 늘어나도 그 판의 기록이 지워지지 않는다.
  - 드랍표: 소형 경험치 구슬(5) 82% / 힐팩 10% / 골드 더미(3G) 5% / 자석 3%. 골드는 판을 넘어 남는 유일한 자원이라 희소하게 두어 항아리가 메타 진행 파밍 수단이 되지 않게 했다. 자석은 **판당 2개 상한**이고, 상한을 넘긴 자석 롤은 **골드가 아니라 경험치로** 떨어진다 — 골드로 흘리면 상점 수입이 자석 운에 좌우된다.
  - **자석**: 아레나에 떨어져 있는 모든 픽업을 즉시 플레이어에게 끌어온다. 시간제 반경 버프가 아니라 일회성 전체 수거 — 싸워서 번 구슬이 한꺼번에 몰려오는 순간이 이 아이템의 가치이기 때문.
  - **힐팩**: 최대 체력의 10%를 회복한다. 고정 수치로 두면 최대 체력 패시브를 쌓은 후반에 반올림 오차가 되기 때문. 체력이 가득 차 있으면 먹히지 않고 바닥에 남아서, 나중에 다시 주우러 올 수 있다.
  - 항아리는 Enemy 레이어/태그를 그대로 쓴다. 그래야 무기 코드를 하나도 고치지 않고 전부가 항아리를 깰 수 있다. 대신 `BreakableRegistry`에 콜라이더를 등록해 **자동 조준에서는 제외**한다 — 안 그러면 궁수가 몰려오는 무리를 두고 장식품에 화살을 쏟아붓는다.
  - 배치는 `PotScatterer`/`PotField`가 씬 로드 시 시드 고정으로 `Resources/Props/Prop_Pot`에서 생성한다(`ArenaBuilder`와 같은 방식이라 두 씬이 동일한 아레나를 얻는다).
- 지형은 `ArenaBuilder`가 씬 로드 시 시드 고정으로 생성한다. 바닥 약 3만 칸은 Tilemap(청크 배칭), 돌담 440개와 장식은 SpriteRenderer. 씬에 커밋하면 편집하지도 않을 오브젝트로 씬 파일이 비대해지고, 시드가 고정이라 매 판·두 씬에서 배치가 동일해 같은 장소로 읽힌다.

---

## 군중 물리

적 무리는 "뚫고 나가는 대상"이지 벽이 아니다. 그 감각을 만들기 위해 물리와 피격 판정을 분리했다.

- 플레이어 바디 콜라이더는 몸통 크기까지 줄이고(0.5 → 0.25), **피격 판정은 별도 트리거(`PlayerHurtbox`)로 분리**했다. 바디는 논트리거라 적을 물리적으로 밀어내는데, 피격 범위가 바디보다 작으면 아예 맞지 않는 완전 무적이 되어 버린다. 둘을 분리해 두면 밀어내기용 물리 반경을 자유롭게 조정해도 "얼마나 맞는가"가 조용히 바뀌지 않는다.
- 무기 타격은 적을 밀어낸다(`ApplyKnockback`). 밀린 적이 뒤에 있는 적을 밀고, 그 밀림이 무리 전체로 전파되는 것이 핵심 — 한 마리가 뒤로 가는 것 자체가 목적이 아니다. 넉백 중엔 추적 조향을 잠시 멈춰 밀림이 상쇄되지 않게 한다.
- 상태 이상은 **하드 CC(스턴)와 소프트 CC(둔화)를 다르게 합산**한다. 스턴은 긴 쪽이 이기고, 둔화는 가장 강한 것 하나만 적용하며 재적용 시 지속시간만 갱신한다 — 곱연산으로 쌓이면 연쇄형 무기 하나가 무리를 정지시켜 버린다.
- 제자리에 고정된 적(보스 윈드업 등)은 같은 적 레이어와의 충돌을 잠시 끈다. 100마리가 한 점에 기대면 보스가 자기 경고 원 밖으로 밀려나고 무리도 서로 끼기 때문. 플레이어 레이어는 그대로라 몸은 여전히 단단하고 피격도 정상 작동한다.
- 화면에서 크게 벗어난 적은 삭제하지 않고 플레이어 주변으로 **재배치(리사이클)** 한다 — 압박과 그 적이 들고 있는 경험치/골드가 그대로 유지되고, 거리가 충분해서 이동이 눈에 보이지 않는다.

---

## 결과 화면 / 타이틀 화면

- **결과 화면**: 두 엔딩이 서로 반대 모양의 연출을 갖는다(`ResultSequence`). 사망은 히트스톱 뒤 시간이 늘어지며 카메라가 시체로 파고들고 화면이 어두워진 다음에야 멈추고, 클리어는 반대로 시간을 **돌려줘서** 카메라가 물러나고 남은 적이 타서 사라지며 바닥의 픽업이 전부 빨려 들어온 뒤에 패널이 뜬다(그 사이 플레이어는 무적).
- 결과 패널은 생존 시간 / 도달 레벨 / 처치 수 / 획득 골드(+클리어 보너스)를 한 줄씩 0부터 카운트업하며 밝히고, 숫자가 다 올라갈 때까지 버튼 입력을 잠근다. 그 뒤 "다시 시작"(같은 씬 재시작) / "타이틀로"(타이틀 씬 이동).
  - 도달 레벨은 **연출 시작 시점에 스냅샷**한다 — 클리어 수거로 쏟아져 들어온 경험치까지 세면 싸워서 올린 레벨이 아니게 되기 때문. 반대로 골드는 수거 후에 읽는다(그 코인은 실제로 잡은 적이 떨군 것이다).
- **타이틀 화면**: 전용 배경 아트(폐허/여명 레이어), 캐릭터 3종 동적 목록(해금 여부에 따라 "선택"/"선택됨"/"해금" 버튼 전환, 선택된 캐릭터는 슬롯 테두리가 바뀜), 보유 골드 표시, 골드 상점(영구 패시브 9종 스크롤 목록), "게임 시작", 테스트용 "초기화"(`PlayerPrefs.DeleteAll`) 버튼.
- 캐릭터 목록과 상점 목록 모두 같은 `ShopSlot` 프리팹을 재사용하는 동적 리스트라 항목이 늘어나도 UI를 손댈 필요가 없다. 목록이 뷰포트를 넘으면 `SlotListView`가 스크롤로 전환하고, 각 슬롯은 현재 레벨을 칸 게이지로 표시한다(칸 수는 업그레이드의 최대 레벨에 맞춰 런타임 생성).

---

## 피드백/연출

- 전방 베기·보스 슬램의 판정 범위는 절차적으로 생성한 부채꼴/원형 메쉬로 표시 — 스프라이트로 대충 흉내내지 않고 실제 `Physics2D.OverlapCircleAll` 판정 반경·각도와 정확히 같은 도형을 매 공격마다 그려서, 시각적으로 보이는 범위와 실제 맞는 범위가 절대 어긋나지 않는다(추후 최종 스프라이트로 교체 예정인 임시 색상 도형).
- 적이 데미지를 받으면 머리 위로 데미지 숫자가 떠올랐다 서서히 사라짐(`TextMesh` 기반 월드 스페이스 텍스트, 로그가 아니라 실제 화면 표시).
- 대시(액티브 스킬)는 화면 우하단 버튼으로 발동하며, 같은 버튼이 쿨타임 원형 게이지 역할도 겸한다(쿨타임 중엔 비활성화) — 해당 무기가 없는 캐릭터로 플레이할 땐 자동으로 숨겨진다.
- 대규모 웨이브/보스 등장 시 화면 중앙에 "WAVE n"/"BOSS" 문구가 떴다 서서히 사라지는 안내 배너.
- 보스 등장 시에만 나타나는 전용 체력바(평소엔 숨겨져 있다가 보스가 스폰되는 순간 바인딩되어 표시) — 체력 구간에 따라 4단계 스프라이트로 교체된다.
- **버프 아이콘 줄**(`BuffIconBarUI`): HP 바 오른쪽에 전사 레이지 스택 수, 마법사 힐/블레싱 남은 쿨다운을 radial fill로 표시. 슬롯은 런타임 생성이라 씬에는 컴포넌트와 스프라이트 참조만 둔다.
- **UI 아트**: 버튼은 9-slice 픽셀 아트 프레임이며 호버/눌림/비활성이 SpriteSwap으로 전환된다. 레벨업 카드는 마우스를 올린 카드만 테두리가 강조 프레임으로 바뀌어 어느 것을 고르는 중인지 드러난다. 전체 화면을 덮는 패널(레벨업/결과/상점)은 프레임이 아니라 반투명 스크림이라 뒤의 게임 화면이 비친다.
- **드랍 픽업**: 경험치 구슬은 획득량에 따라 3단계로 크기·색이 바뀐다(일반/빠름 → 작은 청록, 탱커 → 중간 보라, 보스 → 큰 자홍). 프리팹과 오브젝트 풀은 하나만 쓰고 `SetAmount()`가 스프라이트를 고른다. 경험치는 마름모, 골드는 원으로 실루엣을 통일해서 화면에 수십 개가 겹쳐도 종류가 갈린다.
- **그리기 순서**: 스프라이트를 9개 Sorting Layer(`Ground → Decal → Prop → Wall → Pickup → Character → Effect → Overlay`)로 나누고, 레이어 안에서의 앞뒤는 커스텀 정렬 축 `(0, 1, 0)`이 결정한다 — 화면상 아래에 선 쪽이 앞에 그려지는 탑다운 깊이감. 플레이어와 적을 굳이 한 레이어(`Character`)에 묶은 이유는, 플레이어를 위 레이어로 빼면 **앞에 서 있는 적이 플레이어 뒤로 숨어** 깊이가 거꾸로 읽히기 때문이다. 플레이어의 물리 반지름(0.20)이 스프라이트 폭(0.56)보다 한참 작아서 몸이 겹치는 것은 피할 수 없고, 그렇다면 겹칠 때 자연스럽게 보이는 쪽을 택하는 편이 낫다.
- **개발 편의**: 테스트 전용 도구는 `Scripts/Debug/` 한 폴더에 모아 두고 출시 전 통째로 지울 수 있게 했다(폴더 안 `README.md`에 삭제 절차 기재). F1 홀드로 10배속, F2로 보스 즉시 소환, 레벨업/무기 강제 진화 버튼, 타이틀의 `+100 G` 버튼, 경과 시간·생존 적 수·레벨·FPS 오버레이 — 전부 에디터·개발 빌드에서만 동작한다.

---

## 스코프

### 1개월 버전 (초기 계획, 유지된 부분)

- 8방향 이동, 체력/피격/사망
- 경험치 + 레벨업 카드 선택 (3장 중 1장)
- 적 3종 (기본, 빠름, 탱커)
- 시간 기반 스폰 웨이브
- 기본 UI (HP, 경험치, 타이머), 결과 화면

### 1개월 버전에서 제거했던 것

- 캐릭터 선택 화면 → *(아래 확장에서 다시 도입됨)*
- 원거리 적 → 제외
- 보스 → 제외 계획이었으나 *(아래 확장에서 결국 도입됨)*

### 확장 스코프 (원 스코프 밖, 포트폴리오 볼륨을 위해 진행 중)

- **캐릭터 시스템**: 전사/궁수/마법사 3종, 골드로 영구 해금, 캐릭터별 전용 무기 3개(공유 없음) + 캐릭터별 기본 스탯(체력/방어력/이동속도/쿨감) 차등으로 정체성 분리
- **무기 레벨링**: "해금" 이분법 대신 레벨업 카드를 먹을 때마다 레벨이 올라 데미지·쿨타임이 강화되는 뱀서라이크식 성장, 레벨업 카드는 이미 투자한 무기/패시브에 가중치를 더 줌
- **런 패시브 / 영구 패시브 분리**: 이번 판 한정 스탯 강화(레벨업 카드)와 세션 간 영구 강화(골드 상점)를 구조적으로 분리
- **범용 투사체/범위 패시브**: "투사체 증가"/"범위 증가"를 여러 무기가 공용으로 소비하도록 설계해, 새 무기를 추가해도 기존 패시브가 자동으로 적용됨
- **타이틀 화면**: 캐릭터 선택/해금 + 골드 상점을 결과 화면에서 분리해 별도 씬으로 이동
- **웨이브 & 보스**: 90초 간격 대규모 웨이브 6회 + 10분 보스 등장, 보스 처치를 클리어 조건으로 변경, 보스 전용 슬램 패턴·체력바·안내 배너
- **항아리 & 소비형 픽업**: 시간에 따라 늘어나는 파괴 가능 항아리 + 드랍표(자석/힐팩/골드/경험치), 시야 밖 생성으로 "돌아다니는 것"에 보상
- **자석, 데미지 숫자 팝업, 대시 쿨타임 게이지, 결과 연출(히트스톱·슬로모·수거 시퀀스)** 등 QoL/피드백 요소
- **아레나 & 아트 파이프라인**: 무한 맵을 반지름 70 원형 아레나로 제한, 바닥/돌담/장식/픽업/UI를 aseprite-mcp로 직접 생성해 배선 (`docs/art-plan.md` 4장) — 아트는 전부 직접 제작하고 무료 에셋은 쓰지 않는다
- **결과 연출**: 사망/클리어에 서로 반대되는 엔딩 시퀀스와 카운트업 결과 패널(`ResultSequence`)
- **렌더링 정렬 정비**: Default 한 레이어에 sortingOrder 숫자로만 버티던 그리기 순서를 Sorting Layer 9종으로 분리하고, 커스텀 정렬 축(Y)으로 탑다운 깊이감을 켬
- **행운 → 크리티컬**: 스탯만 있고 소비처가 없던 행운을 크리티컬 확률로 연결, 카드+상점 합계 50% 상한, 도트/고정 프록은 제외
- **보스전 심화**: 슬램 + 운석 2패턴, 공통 경고(`BossTelegraph`)와 패턴 겹침 방지 게이트, 보스 전용 스프라이트/애니메이션, 구간별 보스 체력바, 하드 CC 면역
- **군중 물리 리워크**: 피격 판정 분리(`PlayerHurtbox`), 넉백 전파형 무리, 하드/소프트 CC 합산 규칙 분리, 화면 밖 적 리사이클
- **무기 진화**: 9개 무기 전부 레벨 8(만렙) 도달 시 강제로 등장하는 진화 카드로 완전히 다른 무기로 전환(레벨 1부터 재성장) — 물리/마법 방어력 분리, 관통력 스탯, 적 임시 디버프(둔화/발화/독), 진화 전후로 끊김 없는 액티브 스킬 UI 분리(대시), 임시 버프 시스템(블레싱) 등 새 인프라를 이 과정에서 함께 구축

---

## 주차별 일정 (1개월 버전 기준)

> 빌드(플랫폼 빌드 세팅·실기 검증)는 진행하지 않기로 결정해 스코프에서 제외했다.

| 주차 | 목표                                           | 상태                                     |
| ---- | ---------------------------------------------- | ---------------------------------------- |
| 1주  | 플레이어 이동·애니메이션, 적 1종 스폰·추적   | ✅ 완료 (애니메이션 제외 — 아트 미제작) |
| 2주  | 무기 구현, 오브젝트 풀링, 충돌 처리        | ✅ 완료                                  |
| 3주  | 레벨업 시스템, 업그레이드 카드 UI, 적 3종 완성 | ✅ 완료                                  |
| 4주  | 결과 화면, 버그 수정                           | ✅ 완료 (타이머/결과 화면/영구 재화·상점) — 빌드는 스코프에서 제외 |
| 확장 | 캐릭터 시스템, 직업별 무기, 무기 진화, 타이틀 화면, QoL, 웨이브/보스 | 🟡 진행 중 (무기 및 진화 9종 전부 완료, 웨이브/보스 및 보스 2패턴 완료, 군중 물리·경험치 곡선·밸런싱·결과 연출 완료, 사운드 남음) |

---

## 구현 현황

### 완료

- **플레이어**: 8방향 이동(Input System 기반, 키보드 + 온스크린 가상 조이스틱), 체력/피격(무적 프레임)/사망, 스탯 시스템(`PlayerStats`)
- **모바일 입력**: 화면 터치 지점에 동적으로 생성되는 가상 조이스틱, 레벨업 카드·UI 전부 마우스/터치 클릭 지원
- **카메라**: Cinemachine 추적 카메라
- **적 3종 + 보스**: 기본/빠름/탱커 — 추적 AI, 접촉 데미지, 체력(중복 사망 처리 가드 포함), 오브젝트 풀링. 스포너가 가중치 배열로 종류를 뽑아 종류 추가가 쉬움. 보스는 위 구성 요소를 그대로 재사용하되 풀링 없이 단일 인스턴스로 생성, 전용 스프라이트/Walk 애니메이션 + 슬램·운석 2패턴(`BossSlamAttack`/`BossMeteorAttack`) + 공통 경고 도형(`BossTelegraph`) + 패턴 겹침 방지(`BossPatternGate`) + 하드 CC 면역(`EnemyStatusImmunity`)
- **캐릭터 시스템**: 전사/궁수/마법사 3종, `CharacterDefinition` 전략 패턴, 골드 영구 해금, 캐릭터별 전용 무기 자동 등록/격리(`ILevelableWeapon.IsAvailable`), 캐릭터별 기본 스탯 보너스(체력/방어력/이동속도/쿨감)로 정체성 분리
- **무기**: 전사 3종(전방 베기/공전검/흡혈), 궁수 3종(활쏘기/화살비/대시), 마법사 3종(낙뢰/화염구/힐) — `LevelableWeapon` 공통 레벨링 프레임워크, 적 자동 조준(`EnemyTargeting`, 단일/다중 타겟 모두 지원), 오브젝트 풀 기반 투사체
- **무기 진화 9종 전부 완료**(콤보 어택/소드 체이싱/레이지/집중/난사/맹독 대시/아이스 볼트/익스플로전/블레싱) — 레벨 8 만렙 도달 시 강제 진화 카드, `EvolutionTarget`/`Retire()` 공용 인프라 재사용. 진화 과정에서 물리/마법 방어력 분리(`EnemyHealth`), 통합 관통력 스탯(`PlayerStats.ArmorPenetration`/`MagicPenetration`), 적 임시 디버프(둔화·발화·중독을 각각 `EnemyChaser`/`EnemyHealth`에 추가), 액티브 스킬 UI/무기 분리(`IActiveRoll` 인터페이스로 대시→맹독 대시 진화 전후 UI 끊김 방지), 시간제한 버프 시스템(`PlayerStats.ApplyTemporaryBuff`)까지 함께 구축
- **경험치/레벨업**: 적 처치 시 경험치 획득 → 레벨업 시 타임스케일 정지 + 카드 3장(무기 레벨업 + 런 패시브 혼합, 이미 투자한 항목에 가중치 부여, 중복 없이 랜덤 추출) 중 1장 선택
- **런 패시브 9종**: 체력/이동속도/방어력/주 능력치/쿨타임 감소/행운/투사체 증가/범위 증가/관통력 — 이번 판 한정, `RunPassive` 전략 패턴
- **영구 패시브 상점 9종**: 체력/공격력/마력/이동속도/방어력/쿨타임 감소/자석/관통력/행운 — `PlayerPrefs` 영구 저장, `PassiveUpgrade` 전략 패턴, 동적 리스트 UI
- **타이틀 화면**: 캐릭터 선택/해금 동적 목록, 골드 상점, 초기화 버튼
- **웨이브 & 보스**: 시간 기반 난이도 곡선(스폰 속도·적 체력/방어·경험치 드랍량 증가), 진행 방향 편향 스폰, 90초 간격 대규모 웨이브 6회(빠른 무리 3 + WAVE 3), 10분 보스 등장 + 구간별 전용 체력바 + 슬램/운석 패턴, 보스 처치 기반 클리어 조건
- **군중 물리**: 플레이어 피격 판정 분리(`PlayerHurtbox`), 넉백이 무리로 전파되는 밀어내기, 하드 CC(스턴)/소프트 CC(둔화) 합산 규칙 분리, 고정 상태 적의 동족 충돌 해제, 화면 밖 적 리사이클
- **경험치 드랍량 곡선**: 적이 드랍하는 경험치량을 시간 경과에 따라 5단계로 1.5배→2.6배까지 상향(`EnemySpawner.GetExperienceMultiplier`, 600초 램프). 필요 경험치는 `10 × 레벨` 선형 그대로 — 목표였던 "10분에 50레벨 안팎"을 플레이 실측으로 확인 완료
- **밸런싱**: 캐릭터 3종 성능 균형, 진화 무기 수치, 보스 2패턴(슬램/운석)의 데미지·주기·경고 시간 조정 완료
- **결과 화면**: 엔딩별 연출(사망 = 히트스톱→슬로모→암전, 클리어 = 시간 복귀→적 소각→픽업 전량 수거), 생존 시간/도달 레벨/처치/획득 골드 카운트업, "다시 시작"/"타이틀로"
- **드랍 픽업**: 경험치 100% 드랍, 골드는 적 종류별 확률 드랍(보스는 확정 드랍), 자석 스탯으로 자동 흡인
- **항아리 & 항아리 드랍**: 파괴 가능 오브젝트(`BreakablePot`), 한 방 파괴, 시간에 따라 개수가 늘어나는 필드(`PotField` — 시작 30 + 20초마다 1 + 60초마다 5, 항상 시야 밖 생성, 잔해는 영구히 남음), 드랍표(경험치 82%/힐팩 10%/골드 5%/자석 3%, 자석은 판당 2개 상한·초과분은 경험치), 자동 조준 제외(`BreakableRegistry`)
- **소비형 픽업 2종**: 자석(`MagnetPickup` — 필드의 모든 픽업 즉시 수거), 힐팩(`HealthPackPickup` — 최대 체력 10% 회복, 만피 시 미획득)
- **피드백**: 실제 판정 범위와 일치하는 절차적 메쉬 인디케이터, 데미지 숫자 팝업, 대시 쿨타임 게이지, 웨이브/보스 안내 배너, 구간별 보스 체력바, 버프 아이콘 줄(레이지 스택 / 힐·블레싱 쿨다운)
- **UI**: HP 바, 경험치 바, 타이머, 레벨업 카드 패널, 결과 화면, 상점/캐릭터 스크롤 리스트(레벨 게이지 포함), 가상 조이스틱, 보스 체력바, 웨이브/보스 안내 배너 — 전부 9-slice 픽셀 아트 프레임 적용
- **아레나**: 반지름 70 원형 경계(위치 클램프 + 벽면 슬라이드), 아레나 밖 스폰을 안쪽으로 반사, `ArenaBuilder`가 바닥 Tilemap·돌담 440개·장식·마모 띠·중앙 문양을 시드 고정으로 런타임 생성
- **크리티컬**: 행운 스탯 → 크리티컬 확률(상한 50%), 배율 200%, 공격 1회당 1롤, 금색 확대 데미지 숫자. 도트 틱과 고정 데미지 프록은 제외
- **안전장치**: 15분 시간 제한(보스 등장 후 카운트다운 + 60초 전 경고 배너), 레벨업 카드 풀이 고갈되면 남은 칸을 골드 카드로 채움 — 이론상 정상 플레이에서는 닿지 않지만 고갈 시 카드 슬롯이 비는 문제를 막는다
- **렌더링 정렬**: Sorting Layer 9종 + 커스텀 정렬 축(Y) 기반 그리기 순서. 레이어 이름은 `SortingLayers` 상수로 모아 두고, `sortingOrder`는 같은 레이어 안의 예외에만 쓴다(예: `Decal`의 마모 띠 0 → 중앙 문양 1 → 화염 장판 2 → 보스 슬램 경고 3)
- **사운드**: 배경음 재생기(`Resources/BGM` 자동 로드, `DontDestroyOnLoad`로 씬 전환·재시작에도 끊기지 않음) — 음원 파일은 미투입

### 미완료

- **사운드** — 배경음 재생기는 있으나 `Resources/BGM` 음원 파일 미투입, 효과음 미착수

### 스코프에서 제외

- **빌드** — 플랫폼 빌드 세팅과 실기 검증은 진행하지 않기로 결정했다. 조작·UI(온스크린 조이스틱, 터치 입력, 액티브 스킬 버튼)는 처음부터 모바일을 전제로 설계했지만, 실제 패키징까지는 이 프로젝트의 목표가 아니다.

---

## 기술 스택

| 분류   | 내용                            |
| ------ | ------------------------------- |
| 엔진   | Unity 6000.4.3f1                |
| 대상   | 모바일 전제로 UI·조작 설계 (에디터 플레이 기준, 플랫폼 빌드는 스코프 제외) |
| 렌더링 | URP (Universal Render Pipeline) |
| 입력   | Unity Input System (키보드 + 온스크린 가상 조이스틱) |
| 카메라 | Cinemachine                     |
| 타일맵 | `com.unity.2d.tilemap` (아레나 바닥 약 3만 칸을 런타임 생성) |
| 저장   | `PlayerPrefs` (골드, 캐릭터 해금, 영구 패시브 레벨, 선택 캐릭터, 배경음 볼륨) |
| 언어   | C#                              |
| 아트   | 전부 직접 제작(외부 에셋 미사용) — Aseprite 기반, 캐릭터/적/보스는 PixelLab 생성 후 후가공, 이펙트는 GPT·Gemini 레퍼런스 후 선 정리, UI·타일·픽업은 aseprite-mcp로 직접 생성 (`docs/art-plan.md`) |

---

## 프로젝트 구조

```
Assets/_Project/Scripts/
├── Player/     이동(키보드+조이스틱), 체력/피격(PlayerHurtbox로 판정 분리)/사망, 경험치·레벨, 스탯(PlayerStats)
├── Enemy/      추적 AI·군중 물리(넉백/스턴/둔화), 체력(이벤트 기반 체력바 연동), 접촉 데미지 (기본/빠름/탱커 3종),
│               보스 패턴(슬램/운석) + 공통 경고(BossTelegraph) + 패턴 게이트 + CC 면역
├── Weapon/     캐릭터별 전용 무기(LevelableWeapon 상속), 오브젝트 풀, 투사체, 적 타겟팅
├── LevelUp/    런 패시브(RunPassive, 전략 패턴), 레벨업 카드 매니저(가중치 기반 카드 추첨)
├── Arena/      아레나 경계(ArenaBounds), 지형 런타임 생성(ArenaBuilder), 항아리 배치·리사이클(PotScatterer/PotField)
├── Rendering/  Sorting Layer 이름 상수(SortingLayers)
├── Audio/      배경음 재생기(BackgroundMusic)
├── Spawner/    적 스폰(오브젝트 풀 기반), 대규모 웨이브·보스 스폰 트리거
├── UI/         HP 바, 경험치 바, 가상 조이스틱, 대시 쿨타임 게이지, 데미지 숫자 팝업, 보스 체력바,
│               결과 연출(ResultSequence),
│               버프 아이콘 줄(BuffIconBarUI), 웨이브/보스 안내 배너
└── Game/       캐릭터 정의(CharacterDefinition 전략 패턴), 타이머·결과 화면(GameManager),
                골드/경험치 픽업 + 자석, 항아리(BreakablePot)·드랍표(PotDropTable)·소비형 픽업(자석/힐팩),
                영구 패시브·상점(ShopManager), 타이틀 화면(TitleManager)

Assets/_Project/Data/
├── Weapons/    무기 스탯 ScriptableObject (캐릭터별)
├── Upgrades/   런 패시브 ScriptableObject
├── Passives/   영구 패시브 ScriptableObject (골드 상점)
└── Characters/ 캐릭터 정의 ScriptableObject (전사/궁수/마법사)

Assets/_Project/Textures/
├── Player/     캐릭터 3종 (.aseprite, PPU 50)
├── Enemy/      적 3종 (.aseprite, PPU 50)
├── Effects/    무기 이펙트 (.aseprite, PPU 50·100)
├── UI/         버튼·패널·슬롯 프레임, 바, 보스바 4단계, 버프 아이콘 3종 (48×48, PPU 100, 9-slice 6px)
├── Title/      타이틀 배경 아트
├── Pickups/    경험치 3단계·골드 2종·자석·힐팩 (16×16, PPU 32)
└── Props/      항아리 (정상 + 파괴 프레임)

Assets/_Project/Resources/
└── Ground/     아레나 타일·돌담·장식·중앙 문양 (PPU 32) — 런타임에 이름으로 로드하므로
                씬/프리팹 배선이 필요 없다. 배경음(BGM.*)도 같은 이유로 여기에 둔다

Assets/_Project/Scenes/
├── Title.unity      캐릭터 선택/해금, 골드 상점
├── Game.unity       실제 플레이, 레벨업, 결과 화면
└── TestStage.unity  개발용 테스트 스테이지 — 허수아비 소환 + 방어력/마법방어력 수동 조절 UI로 무기 데미지만 독립적으로 검증(빌드 대상 아님, Game.unity와 Player 구성을 동일하게 유지)
```

---

*(아트 제작 계획과 파이프라인별 함정은 `docs/art-plan.md` 참고)*

---

*Week1~3 완료 + 모바일 입력, 레벨업 시스템 확장성 리팩터링, 타이머/결과 화면, 영구 재화·패시브 상점까지 반영한 뒤 — 원 스코프를 넘어 캐릭터 시스템(전사/궁수/마법사 무기 및 정체성 밸런싱 완료)·직업별 무기 레벨링·타이틀 화면·QoL 피드백·웨이브 및 보스전까지 확장. 이어서 9개 무기 전부의 진화(레벨 8 만렙 시 완전히 다른 무기로 전환)까지 완료 — 그 과정에서 물리/마법 방어력 분리, 통합 관통력 스탯, 적 임시 디버프(둔화·발화·중독), 액티브 스킬 UI 분리, 시간제한 버프 등 새 인프라도 함께 구축. 이후 캐릭터 3종 성능 균형과 진화 무기 밸런싱을 수치로 맞추고, 적 밀도를 올리면서 이동속도와 군중 밀어내기를 조정해 "포위를 뚫고 탈출하는" 감각을 만들었다. 무한 맵을 반지름 70 원형 아레나로 제한한 것도 같은 맥락 — 도망이 영구 해법이 되면 그 감각이 성립하지 않기 때문이다. 아트는 외부 에셋 없이 전부 직접 만들었고(UI·바닥 타일·드랍 픽업은 aseprite-mcp 파이프라인), 소비처가 없던 행운 스탯은 크리티컬로 연결했다. sortingOrder 매직 넘버로 버티던 그리기 순서도 Sorting Layer 9종 + Y축 정렬로 정리했다. 이후 보스전을 슬램+운석 2패턴으로 확장하면서 공통 경고 도형·패턴 겹침 방지·보스 CC 면역 같은 규칙을 세웠고, 무리를 "밀고 뚫는" 감각에 맞춰 피격 판정을 물리 바디에서 분리하고 넉백이 무리로 전파되도록 군중 물리를 다시 짰다. 경험치 드랍량 곡선도 시간 단계식으로 붙였다. 경험치 곡선과 밸런싱은 실측으로 맞췄고, 아레나를 돌아다닐 이유를 만들기 위해 파괴 가능한 항아리와 소비형 드랍(자석/힐팩)을 붙였다. 남은 큰 과제는 사운드다(빌드는 스코프에서 제외).*
